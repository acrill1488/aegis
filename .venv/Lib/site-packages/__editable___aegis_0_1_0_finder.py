from __future__ import annotations
import sys
from importlib.machinery import ModuleSpec, PathFinder
from importlib.machinery import all_suffixes as module_suffixes
from importlib.util import spec_from_file_location
from itertools import chain
from pathlib import Path

MAPPING: dict[str, str] = {'aegis': 'F:\\AEGIS\\aegis', 'assets': 'F:\\AEGIS\\assets', 'backups': 'F:\\AEGIS\\backups', 'compose': 'F:\\AEGIS\\compose', 'config': 'F:\\AEGIS\\config', 'configs': 'F:\\AEGIS\\configs', 'data': 'F:\\AEGIS\\data', 'docs': 'F:\\AEGIS\\docs', 'env': 'F:\\AEGIS\\env', 'examples': 'F:\\AEGIS\\examples', 'knowledge': 'F:\\AEGIS\\knowledge', 'models': 'F:\\AEGIS\\models', 'prompts': 'F:\\AEGIS\\prompts', 'requirements': 'F:\\AEGIS\\requirements', 'scripts': 'F:\\AEGIS\\scripts', 'templates': 'F:\\AEGIS\\templates', 'tests': 'F:\\AEGIS\\tests', 'tools': 'F:\\AEGIS\\tools'}
NAMESPACES: dict[str, list[str]] = {'assets': ['F:\\AEGIS\\assets'], 'backups': ['F:\\AEGIS\\backups'], 'compose': ['F:\\AEGIS\\compose'], 'config': ['F:\\AEGIS\\config'], 'configs': ['F:\\AEGIS\\configs'], 'data': ['F:\\AEGIS\\data'], 'docs': ['F:\\AEGIS\\docs'], 'env': ['F:\\AEGIS\\env'], 'examples': ['F:\\AEGIS\\examples'], 'knowledge': ['F:\\AEGIS\\knowledge'], 'models': ['F:\\AEGIS\\models'], 'prompts': ['F:\\AEGIS\\prompts'], 'requirements': ['F:\\AEGIS\\requirements'], 'scripts': ['F:\\AEGIS\\scripts'], 'templates': ['F:\\AEGIS\\templates'], 'tests': ['F:\\AEGIS\\tests'], 'tools': ['F:\\AEGIS\\tools'], 'aegis.installer.manifests': ['F:\\AEGIS\\aegis\\installer\\manifests'], 'compose.server': ['F:\\AEGIS\\compose\\server'], 'compose.unlimited-ocr': ['F:\\AEGIS\\compose\\unlimited-ocr'], 'compose.workstation': ['F:\\AEGIS\\compose\\workstation'], 'compose.unlimited-ocr.service': ['F:\\AEGIS\\compose\\unlimited-ocr\\service'], 'configs.mcp': ['F:\\AEGIS\\configs\\mcp'], 'configs.n8n': ['F:\\AEGIS\\configs\\n8n'], 'configs.ollama': ['F:\\AEGIS\\configs\\ollama'], 'configs.openwebui': ['F:\\AEGIS\\configs\\openwebui'], 'configs.qdrant': ['F:\\AEGIS\\configs\\qdrant'], 'docs.acceptance': ['F:\\AEGIS\\docs\\acceptance'], 'docs.agents': ['F:\\AEGIS\\docs\\agents'], 'docs.backup': ['F:\\AEGIS\\docs\\backup'], 'docs.history': ['F:\\AEGIS\\docs\\history'], 'docs.images': ['F:\\AEGIS\\docs\\images'], 'docs.network': ['F:\\AEGIS\\docs\\network'], 'docs.releases': ['F:\\AEGIS\\docs\\releases'], 'docs.rfc': ['F:\\AEGIS\\docs\\rfc'], 'docs.security': ['F:\\AEGIS\\docs\\security'], 'docs.server': ['F:\\AEGIS\\docs\\server'], 'docs.workstation': ['F:\\AEGIS\\docs\\workstation'], 'prompts.system': ['F:\\AEGIS\\prompts\\system'], 'scripts.linux': ['F:\\AEGIS\\scripts\\linux'], 'scripts.setup': ['F:\\AEGIS\\scripts\\setup'], 'scripts.windows': ['F:\\AEGIS\\scripts\\windows']}
PATH_PLACEHOLDER = '__editable__.aegis-0.1.0.finder' + ".__path_hook__"


class _EditableFinder:  # MetaPathFinder
    @classmethod
    def find_spec(cls, fullname: str, path=None, target=None) -> ModuleSpec | None:  # type: ignore
        # Top-level packages and modules (we know these exist in the FS)
        if fullname in MAPPING:
            pkg_path = MAPPING[fullname]
            return cls._find_spec(fullname, Path(pkg_path))

        # Handle immediate children modules (required for namespaces to work)
        # To avoid problems with case sensitivity in the file system we delegate
        # to the importlib.machinery implementation.
        parent, _, child = fullname.rpartition(".")
        if parent and parent in MAPPING:
            return PathFinder.find_spec(fullname, path=[MAPPING[parent]])

        # Other levels of nesting should be handled automatically by importlib
        # using the parent path.
        return None

    @classmethod
    def _find_spec(cls, fullname: str, candidate_path: Path) -> ModuleSpec | None:
        init = candidate_path / "__init__.py"
        candidates = (candidate_path.with_suffix(x) for x in module_suffixes())
        for candidate in chain([init], candidates):
            if candidate.exists():
                return spec_from_file_location(fullname, candidate)
        return None


class _EditableNamespaceFinder:  # PathEntryFinder
    @classmethod
    def _path_hook(cls, path) -> type[_EditableNamespaceFinder]:
        if path == PATH_PLACEHOLDER:
            return cls
        raise ImportError

    @classmethod
    def _paths(cls, fullname: str) -> list[str]:
        paths = NAMESPACES[fullname]
        if not paths and fullname in MAPPING:
            paths = [MAPPING[fullname]]
        # Always add placeholder, for 2 reasons:
        # 1. __path__ cannot be empty for the spec to be considered namespace.
        # 2. In the case of nested namespaces, we need to force
        #    import machinery to query _EditableNamespaceFinder again.
        return [*paths, PATH_PLACEHOLDER]

    @classmethod
    def find_spec(cls, fullname: str, target=None) -> ModuleSpec | None:  # type: ignore
        if fullname in NAMESPACES:
            spec = ModuleSpec(fullname, None, is_package=True)
            spec.submodule_search_locations = cls._paths(fullname)
            return spec
        return None

    @classmethod
    def find_module(cls, _fullname) -> None:
        return None


def install():
    if not any(finder == _EditableFinder for finder in sys.meta_path):
        sys.meta_path.append(_EditableFinder)

    if not NAMESPACES:
        return

    if not any(hook == _EditableNamespaceFinder._path_hook for hook in sys.path_hooks):
        # PathEntryFinder is needed to create NamespaceSpec without private APIS
        sys.path_hooks.append(_EditableNamespaceFinder._path_hook)
    if PATH_PLACEHOLDER not in sys.path:
        sys.path.append(PATH_PLACEHOLDER)  # Used just to trigger the path hook
